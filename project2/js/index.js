window.onload = (e) => {
    loadContents();
}

const loadContents = () => {
    // get dropdown button
    const dropdown = document.querySelector(".dropbtn");

    // add event listeners
    dropdown.addEventListener("click", dropdown);
};

const dropdown = (e) => {
};